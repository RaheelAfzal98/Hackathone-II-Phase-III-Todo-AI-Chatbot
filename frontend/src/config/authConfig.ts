// Define the auth configuration for the client
export const AUTH_CONFIG = {
  baseURL: process.env.NEXT_PUBLIC_BETTER_AUTH_URL || 'http://localhost:8002',
  // Add any additional client options here
};